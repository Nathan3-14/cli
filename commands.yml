commands:
  - name: add
    description: Adds the two provided arguments
    args:
      - num_1: float
      - num_2: float
  
  - name: subtract
    description: Subtracts the two provided arguments
    args:
      - num_1: float
      - num_2: float
  
  - name: hello
    description: Says hi!
    args:
      - name: str
      - count: int
        required: false

